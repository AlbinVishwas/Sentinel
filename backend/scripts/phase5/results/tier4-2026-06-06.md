# Tier 4 Golden Path Verification — June 6, 2026

## Result

**PASS** — the full demo rehearsal completed successfully in 24.0 seconds.

- GitLab demo project: `albinvishwas7/sentinel-demo` (private, created fresh for this run)
- GitLab identity: `albinvishwas7`
- Backend: not required (agent invoked in-process)
- Gemini usage: **live** — full agentic loop via Vertex AI (Gemini 3.5 Flash) + GitLab MCP
- Credentials recorded: none
- Merge Request: https://gitlab.com/albinvishwas7/sentinel-demo/-/merge_requests/1

## Commands

Run from `backend/` with `GITLAB_PERSONAL_ACCESS_TOKEN` and Vertex AI ADC configured:

```bash
# Step 1: Create the demo repo + seed the bug + file the issue
.venv/bin/python scripts/phase5/golden_path_setup.py --name sentinel-demo

# Step 2: Timed run + independent REST verification
GOLDEN_DEMO_PROJECT="albinvishwas7/sentinel-demo" \
    .venv/bin/python scripts/phase5/golden_path_run.py --iid 1 --budget 60
```

## Observed Evidence

### `golden_path_setup.py`

- Created private GitLab project `albinvishwas7/sentinel-demo` (HTTP 201).
- Committed buggy file `api/orders.py` to `main` — the `cart_total()` function multiplies subtotal by `len(items)`, overcharging multi-item orders.
- Filed issue #1: *"Cart total is wildly overcharged for multi-item orders"*.
- Printed the exact on-camera prompt and verification command.
- Final verdict: `golden_path_setup: [PASS]`.

### `golden_path_run.py`

- Recorded `main` HEAD before run: `773111030107`.
- Prompt sent: `Fix issue #1 in the GitLab project "albinvishwas7/sentinel-demo".`
- Agent executed the full read→branch→commit→MR loop in **24.0 seconds** (budget: 60s).

**Tool call sequence (5 calls):**

| # | Tool | Duration | Purpose |
| --- | --- | --- | --- |
| 1 | `get_issue` | 982ms | Read issue #1 to understand the bug |
| 2 | `get_file_contents` | 949ms | Read `api/orders.py` to see the buggy code |
| 3 | `create_branch` | 696ms | Create `sentinel/fix-cart-total` from `main` |
| 4 | `create_or_update_file` | 1395ms | Commit the corrected `cart_total()` function |
| 5 | `create_merge_request` | 493ms | Open MR #1 for review |

**Agent's diagnosis:** identified that `cart_total` was multiplying the subtotal by the number of unique items, and corrected it.

**Independent REST API verification:**

- ✅ `sentinel/fix-cart-total` branch created.
- ✅ Merge Request #1 opened from `sentinel/fix-cart-total` → `main`.
- ✅ `main` HEAD unchanged: `77311103` → `77311103`.
- ✅ Elapsed time: 24.0s (well within 60s budget).
- Final verdict: `golden_path: [PASS]`.

## Demo Readiness

The on-camera prompt is:

```
Fix issue #1 in the GitLab project "albinvishwas7/sentinel-demo".
```

The MR for the video: https://gitlab.com/albinvishwas7/sentinel-demo/-/merge_requests/1

## Suite Verdict

```text
PASS  golden_path_setup.py
PASS  golden_path_run.py

GOLDEN PATH DEMO REHEARSAL PASS — 24.0s
```
