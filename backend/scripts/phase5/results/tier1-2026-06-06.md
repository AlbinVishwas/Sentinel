# Tier 1 Deterministic Verification — June 6, 2026

## Result

**PASS** — all deterministic infrastructure tests completed successfully.

- Repository commit before documentation update: `8fd0c26`
- GitLab project: `albinvishwas7/sentinel-test-sandbox`
- GitLab identity: `albinvishwas7`
- Backend: `http://127.0.0.1:8000`
- Gemini usage: none
- Credentials recorded: none

## Command

Run from `backend/` while the FastAPI server is listening on port 8000:

```bash
.venv/bin/python scripts/phase5/run_offline.py
```

## Observed Evidence

### `det_1_secret_manager.py`

- Fake token returned `gitlab_auth_failed` without crashing.
- Empty token returned `gitlab_auth_failed`.
- Auth-shaped runner errors were classified correctly.
- An unrelated error was not misclassified.
- The configured token authenticated as `albinvishwas7`.
- Final verdict: `det_1_secret_manager: [PASS]`.

### `det_2_mcp_isolation.py`

- MCP server handshake succeeded and exposed 58 read-only tools.
- `get_issue` was available and completed without error.
- Issue `#1` returned real GitLab data:
  - Title: `Fix syntax error in utils/parser.py`
  - State: `opened`
  - URL: `https://gitlab.com/albinvishwas7/sentinel-test-sandbox/-/work_items/1`
- Final verdict: `det_2_mcp_isolation: [PASS]`.

### `det_3_streaming.py`

- Parsed 15 valid SSE JSON events.
- Event types: `reasoning`, `tool_call`, `tool_result`, `final`, and `done`.
- Tool calls and results were paired.
- Exactly one `final` event preceded `done`.
- Events arrived incrementally over 5.61 seconds.
- Final verdict: `det_3_streaming: [PASS]`.

## Health Responses

`GET /health`:

```json
{"status":"ok","service":"sentinel-backend"}
```

`GET /health/gitlab`:

```json
{"status":"ok","authenticated":true,"http_status":200,"username":"albinvishwas7","message":"GitLab token valid (authenticated as 'albinvishwas7')."}
```

## Suite Verdict

```text
PASS  det_1_secret_manager.py
PASS  det_2_mcp_isolation.py
PASS  det_3_streaming.py

ALL DETERMINISTIC TESTS PASS
```
